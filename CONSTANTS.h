#pragma once

const float WIDTH = 1280;
const float HEIGHT = 720;

const float RACKET_HEIGHT = (float) HEIGHT * 10.f / 100.f;
const float RACKET_WIDTH = (float) WIDTH * 0.7f / 100.f;

const float SPACE_BEHIND_RACKET = 5.f / 100.f * WIDTH;

const float RADIUS = HEIGHT * 1.3F / 100.F;

const float RACKET_STEPS = 4.5f;
const float BALL_VELOCITY = sqrt(1.4f/ 100.f * WIDTH);